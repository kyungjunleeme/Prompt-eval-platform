apiVersion: v1
kind: Service
metadata:
  name: prompt-hub
  namespace: prompt-eval
spec:
  selector: { app: prompt-hub }
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
