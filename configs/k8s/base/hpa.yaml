apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: prompt-hub
  namespace: prompt-eval
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: prompt-hub
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
